# Cloudflare Workers AI — powers the AI legal chat at /api/chat
# Reuse the same values already configured for daehanlaw-next
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# GraphQL backend (already defaults to api.daehanlaw.com/graphql if unset)
NEXT_PUBLIC_API_URL=https://api.daehanlaw.com/graphql
